# ip-toolbox
Three powerful text processing tools in one page: add prefix/suffix to lines, trim text from delimiter, and group IPs by port with duplicate detection. Built with pure HTML/CSS/JS, no dependencies, auto-save with localStorage.
